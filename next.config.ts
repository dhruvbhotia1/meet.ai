import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    allowedDevOrigins: ["https://tapir-pleasing-mallard.ngrok-free.app", "http://localhost:3000"],
};

export default nextConfig;
